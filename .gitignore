# ============================================================================
# 🔴 密钥 —— 绝不能提交
# auth.json 含 4 个明文 API 密钥（deepseek / opencode-go / zai-coding-cn / kimi-coding）。
# 公开仓库会被爬虫在分钟级抓取，密钥滥用产生的是真实账单。
# ============================================================================
auth.json

# ============================================================================
# 私有运行数据 —— 对话记录（约百 MB 级，日增）
# 含项目细节、简历与实习经历、研究材料等私人内容。
# ============================================================================
sessions/

# ============================================================================
# 第三方内容 —— 可重装（约 0.5 GB 量级）
# 除体积外，把上游技能库并入自己仓库还涉及再分发许可问题。
# 还原方式见 README「还原本配置」一节。
# ============================================================================
skills/
skills-optional/
npm/node_modules/

# ============================================================================
# 含本机绝对路径与用户名，不公开
# ============================================================================
missions/

# ============================================================================
# pi -e 临时扩展的暂存目录（运行时生成）
tmp/

# ============================================================================
# 运行产物 / 系统噪音
# ============================================================================
.DS_Store
._*
__pycache__/
*.pyc
*.log

# Herdr 生成与管理，勿手编；重生成：herdr integration install pi（详见 README）
extensions/herdr-agent-state.ts

# ============================================================================
# 借鉴自 amosblomqvist/pi-config（上游无 LICENSE，不入公开仓库）
# 再获取命令见 README「借鉴组件」一节
# ============================================================================
extensions/bash-guard/
extensions/prompt-snippets/
extensions/md-link.ts
extensions/context.ts

# ============================================================================
# 插件运行产物
# ============================================================================
# @moyai/pi-session-hoarder 的会话存档（含私有对话内容，内容寻址对象 + catalog）
session-hoarder/

# pi-subagents 的宿主机绑定目录（按 cwd 生成；cwd 为本目录时会出现）
.pi/

# ============================================================================
# 指向 dotfiles 的本地符号链接 —— 内容是绝对路径（/Users/sutang/...），
# 一次 git add -A 就会把这台机器的用户名与目录结构作为符号链接提交进公开仓库。
# ============================================================================
AGENTS.md

# ============================================================================
# 私密层元文件：补丁正文（含上游原文），由 pi-config-private 单独跟踪。
# 公开库必须忽略，否则 apply-local-patches 的正文会被 add -A 卷进公开历史。
# ============================================================================
.pi-private/

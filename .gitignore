# ============================================================================
# 🔴 密钥 —— 绝不能提交
# auth.json 含 4 个明文 API 密钥（deepseek / opencode-go / zai-coding-cn / kimi-coding）。
# 公开仓库会被爬虫在分钟级抓取，密钥滥用产生的是真实账单。
# ============================================================================
auth.json

# ============================================================================
# 私有运行数据 —— 75MB 对话记录
# 含项目细节、简历与实习经历、研究材料等私人内容。
# ============================================================================
sessions/

# ============================================================================
# 第三方内容 —— 可重装，当前约 493MB
# 除体积外，把上游技能库并入自己仓库还涉及再分发许可问题。
# 还原方式见 README「还原本配置」一节。
# ============================================================================
skills/
skills-optional/
npm/node_modules/

# ============================================================================
# 含本机绝对路径与用户名，不公开
# ============================================================================
missions/

# ============================================================================
# 运行产物 / 系统噪音
# ============================================================================
.DS_Store
._*
__pycache__/
*.pyc
*.log

# ============================================================================
# Herdr 生成的集成文件 —— 不是本仓库的配置
#   · 文件头声明由 herdr 管理，重装/升级会覆盖，不应手工编辑
#   · 无许可头，无法确认授权；提交进 MIT 仓库会错误标注其许可
#   · 一条命令可重生成：herdr integration install pi
# 注意：只忽略这一个文件，extensions/ 下的自写内容仍需跟踪。
# ============================================================================
extensions/herdr-agent-state.ts

